# Stimulus MI when-analysis uses trial trajectories, not mixed models

Per-trial MI plus animal-level OLS slope and early−late delta (tested across strata) answer “when does coupling change?” without pseudoreplicating trial rows. Rejected: trial-level mixed models (pilot already chose animal scalars), session as primary x (ordinal trial_ord is the exposure clock; session only orders), and replacing pooled animal MI (both endpoints are emitted).
